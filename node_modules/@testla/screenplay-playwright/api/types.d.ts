/**
 * HTTP Headers type
 */
export declare type Headers = {
    [key: string]: string;
};
export declare type ResponseBodyType = unknown;
/**
 * Response type which is returned from any request.
 *
 * @param T (optional) Response body type - usually one out of `Buffer` | `object` | `string` | `null`, defaults to `unknown`.
 */
export declare type Response<T = ResponseBodyType> = {
    body: T;
    status: number;
    headers: Headers;
    duration: number;
};
export declare type ResponseBodyFormat = 'json' | 'text' | 'buffer' | 'none';
