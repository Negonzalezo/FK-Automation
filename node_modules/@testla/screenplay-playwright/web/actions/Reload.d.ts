import { Actor, Action } from '@testla/screenplay';
export declare class Reload extends Action {
    performAs(actor: Actor): Promise<any>;
    /**
     * Reload the current browser page.
     */
    static page(): Reload;
}
