import { FrameLocator, Locator, Page } from '@playwright/test';
export declare type LazySelector = (page: Page | FrameLocator) => Locator;
export declare type Selector = string | Locator | LazySelector;
export declare type FrameSelector = string;
export declare type SelectorOptionsState = 'visible' | 'hidden' | 'attached' | 'detached';
export declare type SubSelector = [
    Selector,
    SelectorOptions?
];
export declare type SelectorOptions = {
    hasText?: string | RegExp | undefined;
    subSelector?: SubSelector;
    timeout?: number;
    state?: SelectorOptionsState;
    evaluateVisible?: boolean;
};
export declare type Maskable = {
    maskInLogs?: boolean;
};
