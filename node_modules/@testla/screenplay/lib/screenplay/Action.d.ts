import { IAction, IActor, ILogable } from '../interfaces';
import { UsingAlias } from '../templates/UsingAlias';
/**
 * Actions can be triggered by calling them from an actor object.
 */
export declare abstract class Action extends UsingAlias implements IAction, ILogable {
    /**
     * Determines if the step can be skipped on failure
     */
    private canSkipOnFail;
    /**
     *  Makes the provided {@link IActor}
     *  perform this Action.
     *
     * @param {IActor} actor
     * @returns {Promise<any>}
     *
     * @see {@link IActor}
     *
     * @override This method will have to be overridden with the actual integration of an action.
     */
    abstract performAs(actor: IActor): Promise<any>;
    /**
     * makes the step pass even if an error is thrown
     */
    get orSkipOnFail(): this;
    /**
     * Returns the orSkipOnFail state
     * @returns if orSkipOnFail is set
     */
    getCanSkipOnFail(): boolean;
}
