import { CallStackCalledWith, CallStackInfo } from '../interfaces';
export declare class UsingLogging {
    private callStack?;
    constructor();
    setCallStackInitializeCalledWith(calledWith: CallStackCalledWith): void;
    addToCallStack(entry: CallStackInfo): void;
    getCallStack(): CallStackInfo[] | undefined;
}
