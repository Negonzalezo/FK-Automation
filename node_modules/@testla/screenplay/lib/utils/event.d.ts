import { LogEvent } from '../interfaces';
export declare const stringifyLogEvent: (event: LogEvent) => string;
export declare const parseLogEvent: (eventString: string) => LogEvent;
export declare const checkIfLogEvent: (eventString: string) => boolean;
